# Shop Detail upload compatibility fix

- Work-item Survey/Design buttons now open in locked context: all selected files auto-map to that exact work item; no redundant board selector.
- Bulk Survey and Bulk Design remain available in the Work Items header and retain per-file mapping.
- Survey upload processes every selected file independently and preserves selection order.
- Added compatibility fallback when `survey_photo_items` is not yet present in the deployed Supabase schema cache: semantic mapping is stored as an empty `board_markings` relationship instead of failing the upload.
- Reads on Shop Detail, Designer, and Reports tolerate an older deployment without `survey_photo_items` and continue using board markings.
- The included `20260918134000_0054_survey_photo_work_item_links.sql` migration remains the preferred permanent schema and backfills existing board-marking links after deployment.
