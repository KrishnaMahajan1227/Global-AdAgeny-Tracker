# Installer photo workflow update

- Installation photo delete now removes only the selected proof row/storage object; work item remains untouched.
- Delete is available inside each expanded approved work item and on final review.
- Added bottom Bulk Gallery Upload: select multiple images, map every image to an exact approved work item, then upload all.
- Per-work-item camera/gallery remains auto-mapped with no repeated work-item selection.
- Review groups installation photos by work item before Final Submit.
